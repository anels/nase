# Change request

Invoice retries must be idempotent. The request identifier is
`DESIGN_RETRY_CANARY_7319`, and the design must retain one target PR unless
repository evidence proves a split is necessary.
